spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/java_react
    driver-class-name: org.mariadb.jdbc.Driver
    username: jun
    password: *
  jpa:
    database-platform: org.hibernate.dialect.MariaDB10Dialect
    open-in-view: true
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update